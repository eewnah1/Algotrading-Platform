[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "algoplatform"
version = "0.1.0"
description = "Institutional-grade open-source algorithmic trading platform"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [{name = "H Tan", email = "eewnah1@gmail.com"}]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Financial and Insurance Industry",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "numpy>=1.24",
    "pandas>=2.0",
    "yfinance>=0.2.40",
    "aiohttp>=3.9",
    "apscheduler>=3.10",
    "python-multipart>=0.0.9",
    "jinja2>=3.1",
    "httpx>=0.27",
    "python-dateutil>=2.9",
    "pytz>=2024.1",
    "scipy>=1.12",
    "scikit-learn>=1.4",
    "ta>=0.11",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.9",
    "black>=24.0",
]

[project.scripts]
algoplatform = "algoplatform.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["algoplatform*"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["DTZ", "BLE", "RUF"]
per-file-ignores = {"__init__.py" = ["F401"]}

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_ignores = true
ignore_missing_imports = true
